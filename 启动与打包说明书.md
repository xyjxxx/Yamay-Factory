# 羊咩的工厂 · 启动与打包说明书

> 豆包视频去水印工具（Windows 桌面应用）  
> 本文档面向两类人：**普通用户**（只想运行软件）和 **开发者 / 维护者**（需要命令行启动、打包分发）。

---

## 目录

1. [软件与环境要求](#一软件与环境要求)
2. [项目目录结构](#二项目目录结构)
3. [启动软件（四种方式）](#三启动软件)
4. [打包为 EXE（三种方式）](#四打包为-exe)
5. [常见问题 FAQ](#五常见问题-faq)

---

## 一、软件与环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64 位） |
| Python | 3.10 或更高（推荐 3.12）。仅源码运行时需要；使用打包好的 EXE 则完全不需要 |
| ffmpeg | 已随项目提供（`ffmpeg\` 目录），缺失时可自行安装 |
| 磁盘空间 | 源码运行约 3~5 GB（含模型）；打包另需数 GB |
| 网络 | 首次安装依赖、下载模型时需要联网 |

---

## 二、项目目录结构

| 路径 | 作用 |
|------|------|
| `启动.bat` | 一键启动脚本（普通用户推荐使用） |
| `打包.bat` | 一键打包脚本（带进度窗口） |
| `requirements-wm.txt` | 全部 Python 依赖清单 |
| `watermark_remover\main.py` | 程序入口 |
| `watermark_remover\build.py` | 打包脚本（PyInstaller，已配置好全部参数） |
| `watermark_remover\build_progress.py` | 打包进度窗口 |
| `watermark_remover\download_ocr_models.py` | 下载 OCR 检测模型 |
| `watermark_remover\download_lama_model.py` | 下载 LaMa 修复模型 |
| `ffmpeg\` | ffmpeg / ffprobe / ffplay 可执行文件 |
| `icons\` | 应用图标（sheep.ico）与动态背景视频（sheep1.mp4） |
| `models\` | OCR 模型 + LaMa 模型（离线打包必需） |
| `dist\` | 打包输出目录（生成的 EXE 在这里） |
| `build\` | 打包中间产物（可删除，不影响使用） |

---

## 三、启动软件

### 方式一：双击启动（普通用户最推荐）

```
双击项目根目录下的  启动.bat
```

- **首次运行**：自动创建虚拟环境（`C:\wm_venv`）→ 自动安装全部依赖 → 自动启动软件
- **之后运行**：直接启动软件
- 首次安装若失败，脚本会提示缺失的依赖并给出处理方法

### 方式二：命令行启动（本机环境已配置好）

适用于已经运行过一次 `启动.bat` 的电脑（虚拟环境 `C:\wm_venv` 已存在）。

**CMD 命令提示符：**

```cmd
cd /d D:\doubao-nomark-main
C:\wm_venv\Scripts\python.exe -m watermark_remover.main
```

**PowerShell：**

```powershell
cd D:\doubao-nomark-main
& C:\wm_venv\Scripts\python.exe -m watermark_remover.main
```

> 提示：`C:\wm_venv` 是 `启动.bat` 创建的项目专用 Python 环境。把项目拷贝到其他电脑后，先运行一次 `启动.bat` 建好环境，之后就能用这两条命令启动。

### 方式三：命令行启动（全新电脑 / 全新环境）

适合在其他电脑上从零开始：

1. 安装 Python 3.10+（安装时勾选 **Add python.exe to PATH**）
2. 打开 CMD，进入项目目录并安装依赖：

   ```cmd
   cd /d D:\doubao-nomark-main
   pip install -r requirements-wm.txt
   ```

3. （推荐）下载检测与修复模型：

   ```cmd
   python watermark_remover\download_ocr_models.py
   python watermark_remover\download_lama_model.py
   ```

4. 确认 ffmpeg：项目自带 `ffmpeg\` 目录即可；若缺失可执行 `winget install ffmpeg`
5. 启动软件：

   ```cmd
   python -m watermark_remover.main
   ```

### 方式四：直接运行打包好的 EXE（最省事，无需任何环境）

```
双击  dist\羊咩的工厂.exe
```

- 无需安装 Python、无需联网、无需任何依赖
- 适合把 EXE 单独拷贝给同事或朋友使用
- 注意：杀毒软件可能误报，需添加信任（见 FAQ）

### 关于命令行参数

当前版本启动命令**不需要也不支持**额外参数，直接运行即可打开主界面。

---

## 四、打包为 EXE

### 打包前置条件（缺一不可，`build.py` 会自动检查）

| 检查项 | 位置 |
|--------|------|
| ffmpeg.exe / ffprobe.exe / ffplay.exe | `ffmpeg\` 目录 |
| 应用图标 sheep.ico | `icons\` 目录 |
| 动态背景视频 sheep1.mp4 | `icons\` 目录 |
| LaMa 模型 big-lama.pt | `models\hub\checkpoints\` 目录 |
| OCR 模型 craft_mlt_25k.pth、zh_sim_g2.pth | `models\easyocr\` 目录 |
| PyInstaller 及全部运行依赖 | 随 `requirements-wm.txt` 一并安装 |

若缺少模型，先执行：

```cmd
python watermark_remover\download_ocr_models.py
python watermark_remover\download_lama_model.py
```

### 方式一：一键打包（推荐）

```
双击项目根目录下的  打包.bat
```

- 弹出打包进度窗口，实时显示进度与日志
- 完成后 EXE 位于 `dist\羊咩的工厂.exe`

### 方式二：命令行打包

**CMD：**

```cmd
cd /d D:\doubao-nomark-main
C:\wm_venv\Scripts\python.exe watermark_remover\build.py
```

或直接：

```cmd
python watermark_remover\build.py
```

### 方式三：手动 PyInstaller（高级，不推荐）

```cmd
cd /d D:\doubao-nomark-main
C:\wm_venv\Scripts\python.exe -m PyInstaller watermark_remover\main.py --onefile --windowed ...
```

> 建议优先使用 `build.py`：它已配置好 ffmpeg、模型、图标、隐藏导入、排除项等全部参数，手动输入容易遗漏，导致打包出的 EXE 缺文件。

### 打包结果说明

| 项目 | 说明 |
|------|------|
| 输出文件 | `dist\羊咩的工厂.exe` |
| 打包方式 | PyInstaller 单文件模式（--onefile） |
| 实际体积 | 约 663 MB（含 OCR + LaMa 模型） |
| 预计耗时 | 5~15 分钟（首次更久） |
| 中间产物 | `build\` 目录与 spec 文件，可删除 |

### 打包后发给别人使用

把 `dist\羊咩的工厂.exe` 单独拷贝给对方即可：

- 对方不需要安装 Python、ffmpeg 或任何依赖
- 双击即可运行
- 首次启动稍慢（单文件模式会先解压到临时目录），属正常现象

---

## 五、常见问题 FAQ

1. **双击 `启动.bat` 闪退 / 窗口一闪而过**  
   多为未安装 Python 或长路径未开启。以管理员身份重新运行 `启动.bat`，脚本会自动开启长路径支持。

2. **pip 安装报错 “No such file or directory”（长路径限制）**  
   PySide6 内部路径较深，Windows 默认 260 字符路径限制会导致安装失败。  
   解决：以管理员身份运行 `启动.bat`；或手动开启后重启电脑：

   ```powershell
   New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem" -Name "LongPathsEnabled" -Value 1 -PropertyType DWORD -Force
   ```

3. **提示找不到 ffmpeg**  
   确保 `ffmpeg.exe`、`ffprobe.exe`、`ffplay.exe` 位于项目根目录 `ffmpeg\` 文件夹下，或执行 `winget install ffmpeg`。

4. **打包报错 “LaMa checkpoint missing”**  
   尚未下载 LaMa 模型。执行：

   ```cmd
   python watermark_remover\download_lama_model.py
   ```

5. **打包报错 “EasyOCR models missing”**  
   尚未下载 OCR 模型。执行：

   ```cmd
   python watermark_remover\download_ocr_models.py
   ```

6. **打包好的 EXE 被杀毒软件拦截 / 删除**  
   单文件 PyInstaller 程序常被误报。将 EXE 所在目录加入杀毒软件信任 / 排除列表即可。

7. **打包后的 EXE 首次启动很慢**  
   正常现象。单文件模式每次启动会先把内容解压到 `%TEMP%`，几秒后即恢复正常速度。

8. **只想用基础功能，不想要 OCR / AI 修复**  
   当前打包脚本默认包含完整功能（OCR 检测 + LaMa 修复）。若确需精简版，需手动修改 `build.py` 并移除对应依赖与模型文件，改动前请确认业务需求。

---

*本文档随项目维护，脚本行为如有变化请同步更新。*
